export default function ith(arr){
    if (arr.length <= 0) {
        return null;
    }
    else{
        var a = {};
        arr.forEach((item) => {
            a.push(item)
            return(a)
        });
    }
}
